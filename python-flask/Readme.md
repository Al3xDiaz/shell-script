### Commands
```
#create virtual env
$ virtualenv vend --python=python3.9.2
# activate virtaul env
$ source /vend/bin/activate
# deactivate virtal env
$ deactivate
# install Flask
$ pip install flask
# show dependencies
$ pip freeze
# install dependencies with file
$ pip install -r requirements.txt
#start proyect 
$ export FLASK_APP=main.py && flask run
```
